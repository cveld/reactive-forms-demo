/** Defines a component to be rendered as a step */
export interface IResource {
    /** The identifier of the resource. */
    id: string;

    /** The value containing the text of the resource. */
    value: string;

    /** The group indentiefier of the resource. */
    groupId: string;
  }
